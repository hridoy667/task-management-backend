generator client {
  provider = "prisma-client"
  output   = "../prisma/generated"
}

datasource db {
  provider = "postgresql"
}

// Your models go here (e.g., User, Task, etc.)
model User { 
  id String @id @default(cuid())
  email String @unique
}